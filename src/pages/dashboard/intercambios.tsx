import { NextPage } from 'next';
import Layout from '../../components/market/layout';

const Intercambios: NextPage = () => {
  return (
    <Layout title="Intercambios">
      <div>Aca debe ir la compra y venta de nfts</div>
    </Layout>
  );
};

export default Intercambios;
