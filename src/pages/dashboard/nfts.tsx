import { NextPage } from 'next';
import Layout from '../../components/market/layout';

const Nfts: NextPage = () => {
  return (
    <Layout title="Nfts">
      <div>aca debe ir los nfts o monedas que tiene el usuario</div>
    </Layout>
  );
};

export default Nfts;
