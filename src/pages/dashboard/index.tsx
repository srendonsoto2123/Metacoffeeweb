import type { NextPage } from 'next';
import Layout from '../../components/market/layout';

const DashBoard: NextPage = () => {
  return (
    <Layout title="Dashboard">
      Aca debe ir el contenido principal de dashboard
    </Layout>
  );
};

export default DashBoard;
